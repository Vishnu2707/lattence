[project]
name = "lattence-crypto"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "cryptography>=45,<51",
  "lattence-core",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lattence_crypto"]
