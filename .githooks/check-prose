#!/bin/sh
set -eu

root=$(git rev-parse --show-toplevel)
pattern=$(cat "$root/.githooks/prose-pattern.txt")

if git grep -nI -i -E "$pattern" -- '*.md'; then
  echo "prose check failed" >&2
  exit 1
fi
